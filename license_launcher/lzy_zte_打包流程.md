# lzy_zte_12.10.exe 许可证验证 - 完整打包流程

## ⚠️ 重要提示

**当前打包结果是 macOS 可执行文件,不是 Windows .exe 文件!**

要生成 Windows .exe 文件,请查看: [生成Windows_EXE指南.md](生成Windows_EXE指南.md)

### 📦 当前打包结果 (macOS 版本)

```
dist/
└── lzy_zte_许可证验证        # 35 MB，macOS 可执行文件
                              # 包含启动器 + lzy_zte_12.10.exe
```

**如需 Windows 版本,请在 Windows 系统上运行 `build_with_program.bat`** 🎉

---

## 🚀 使用流程

### 对于开发者（您）

#### 1. 启动许可证服务器

```bash
cd server
./server
```

服务器将在 `http://localhost:8080` 运行。

#### 2. 生成许可证

访问管理后台：
```
http://localhost:8080/login.html
用户名: lazywords
密码: w7168855
```

点击"生成许可证"，创建测试许可证密钥。

#### 3. 分发文件给用户

将 `dist/lzy_zte_许可证验证` 文件复制给用户。

---

### 对于用户（您的客户）

#### 步骤 1：运行程序

**macOS:**
```bash
./lzy_zte_许可证验证
```

或者双击运行（需要先执行一次 `chmod +x lzy_zte_许可证验证`）

**注意**: 首次运行可能需要在系统设置中允许运行该程序。

#### 步骤 2：输入许可证

程序会自动启动（如果 Tkinter 可用会显示 GUI，否则使用命令行）：

**CLI 模式：**
```
============================================================
  🔐 许可证验证系统
============================================================

请输入许可证密钥:
密钥: [在这里输入许可证]

正在激活许可证...
设备ID: xxx...

============================================================
✅ 许可证激活成功！
============================================================
📅 过期时间: 2026-12-14
📦 产品名称: 标准版

------------------------------------------------------------
🚀 正在启动程序: lzy_zte_12.10.exe
✅ 程序已启动！
------------------------------------------------------------
```

**GUI 模式：**
- 显示图形界面
- 输入许可证密钥
- 点击"激活"按钮
- 自动启动程序

#### 步骤 3：后续使用

再次运行时会自动验证已保存的许可证，无需重新输入。

---

## 🔧 完整打包流程记录

### 打包步骤（已完成）

```bash
# 1. 进入启动器目录
cd license_launcher

# 2. 复制要打包的程序
cp ../examples/lzy_zte/lzy_zte_12.10.exe ./

# 3. 配置参数
cat > launcher_config.json << 'EOF'
{
  "server_url": "http://localhost:8080",
  "target_exe": "lzy_zte_12.10.exe",
  "license_file": ".license",
  "use_gui": "auto"
}
EOF

# 4. 安装依赖
python3.11 -m pip install pyinstaller requests

# 5. 执行打包
python3.11 -m PyInstaller \
    --onefile \
    --name="lzy_zte_许可证验证" \
    --add-data "launcher_config.json:." \
    --add-data "lzy_zte_12.10.exe:." \
    --hidden-import=requests \
    --hidden-import=tkinter \
    --clean \
    launcher.py

# 6. 完成！
# 生成的文件在 dist/lzy_zte_许可证验证
```

### 打包说明

| 参数 | 说明 |
|------|------|
| `--onefile` | 打包成单个可执行文件 |
| `--name` | 指定输出文件名 |
| `--add-data "launcher_config.json:."` | 包含配置文件 |
| `--add-data "lzy_zte_12.10.exe:."` | 包含您的程序 |
| `--hidden-import=requests` | 包含网络请求库 |
| `--hidden-import=tkinter` | 包含 GUI 库（如果可用） |
| `--clean` | 清理临时文件 |

### 文件大小分析

- **启动器**: ~10 MB
- **lzy_zte_12.10.exe**: ~25 MB
- **总大小**: ~35 MB

---

## 💡 工作原理

### 运行时流程

```
用户双击运行
     ↓
启动器检测环境
     ↓
选择 GUI/CLI 模式
     ↓
检查已保存的许可证
     ↓
[如果有] 验证许可证 → 成功 → 释放并启动程序
     ↓
[如果无] 要求输入许可证
     ↓
验证许可证
     ↓
保存许可证到 .license 文件
     ↓
从打包文件中释放 lzy_zte_12.10.exe
     ↓
临时目录：/tmp/lzy_zte_12.10.exe
     ↓
启动程序
     ↓
完成！
```

### 技术细节

1. **打包时**: PyInstaller 将启动器和程序打包到一起
2. **运行时**: 启动器自动检测打包环境（`sys._MEIPASS`）
3. **释放**: 将内嵌的 lzy_zte_12.10.exe 复制到临时目录
4. **验证**: 连接服务器验证许可证
5. **启动**: 使用 Wine（macOS/Linux）或直接运行（Windows）

---

## 📋 文件清单

### 开发文件（license_launcher/）

```
license_launcher/
├── launcher.py                    # 核心代码
├── launcher_config.json           # 配置文件
├── lzy_zte_12.10.exe             # 原始程序
├── build_with_program.sh          # 自动打包脚本
├── requirements.txt               # 依赖列表
└── README.md                      # 文档
```

### 打包结果（dist/）

```
dist/
└── lzy_zte_许可证验证             # 单个可执行文件（分发这个）
```

### 临时文件（可删除）

```
build/                             # 打包临时文件
*.spec                            # PyInstaller 配置
__pycache__/                      # Python 缓存
```

---

## 🎯 分发给用户

### 方案 1：单文件分发（最简单）

```
给用户/
└── lzy_zte_许可证验证
```

### 方案 2：带说明文档

```
给用户/
├── lzy_zte_许可证验证
└── 使用说明.txt
```

### 使用说明.txt 示例

```
====================================
lzy_zte 许可证验证程序
====================================

1. 双击运行"lzy_zte_许可证验证"

2. 首次使用请输入许可证密钥
   (密钥由提供方提供)

3. 验证成功后程序将自动启动

4. 再次使用时会自动验证，无需重新输入

====================================
常见问题
====================================

Q: macOS 提示"无法打开"？
A: 右键点击 → 打开 → 仍要打开

Q: 如何重新激活？
A: 删除生成的 .license 文件

Q: 许可证验证失败？
A: 请联系技术支持

技术支持: [您的联系方式]
====================================
```

---

## 🔒 安全提示

### 当前配置

```json
{
  "server_url": "http://localhost:8080",     // ⚠️ 本地测试地址
  "target_exe": "lzy_zte_12.10.exe",
  "license_file": ".license",                // ✅ 隐藏文件
  "use_gui": "auto"                          // ✅ 自动选择
}
```

### 生产环境建议

修改 `launcher.py` 第 29 行：

```python
CONFIG = {
    'server_url': 'https://your-domain.com',  # 使用 HTTPS
    'target_exe': 'lzy_zte_12.10.exe',
    'license_file': '.license',
    'use_gui': 'auto',
}
```

然后重新打包。

---

## 🛠️ 自定义打包

### 修改程序名称

```bash
--name="您的程序名"
```

### 添加图标（可选）

```bash
# macOS
--icon=icon.icns

# Windows
--icon=icon.ico
```

### 隐藏控制台窗口（Windows）

```bash
--noconsole
```

### 完整命令示例

```bash
python3.11 -m PyInstaller \
    --onefile \
    --noconsole \
    --name="我的程序" \
    --icon=myicon.ico \
    --add-data "launcher_config.json;." \
    --add-data "lzy_zte_12.10.exe;." \
    --hidden-import=requests \
    --hidden-import=tkinter \
    launcher.py
```

---

## 📊 测试清单

### 基本测试

- [x] 打包成功
- [ ] 首次运行（输入许可证）
- [ ] 许可证验证通过
- [ ] 程序成功启动
- [ ] 再次运行（自动验证）
- [ ] 删除 .license 后重新激活

### 异常测试

- [ ] 输入错误的许可证
- [ ] 服务器未运行
- [ ] 网络断开
- [ ] 许可证过期

### 平台测试

- [x] macOS (已测试)
- [ ] Windows
- [ ] Linux

---

## 🐛 故障排查

### 问题 1：无法运行

**macOS:**
```bash
chmod +x dist/lzy_zte_许可证验证
./dist/lzy_zte_许可证验证
```

或右键 → 打开 → 仍要打开

### 问题 2：许可证验证失败

**检查：**
1. 服务器是否运行？
   ```bash
   lsof -i :8080
   ```

2. 配置是否正确？
   ```bash
   # 服务器地址应该是可访问的
   ```

### 问题 3：程序无法启动

**macOS/Linux:**
需要 Wine 来运行 Windows 程序：
```bash
brew install wine-stable
```

### 问题 4：Tkinter 警告

这是正常的。如果 Tkinter 不可用，程序会自动降级到命令行模式。

---

## 🔄 更新流程

### 更新程序

```bash
# 1. 替换新的程序文件
cp new_version.exe license_launcher/lzy_zte_12.10.exe

# 2. 重新打包
python3.11 -m PyInstaller lzy_zte_许可证验证.spec

# 3. 分发新文件
cp dist/lzy_zte_许可证验证 /path/to/distribution/
```

### 更新配置

编辑 `launcher_config.json`，然后重新打包。

---

## 📞 技术支持

### 日志位置

- 许可证文件：当前目录的 `.license`
- 临时文件：`/tmp/lzy_zte_12.10.exe`

### 调试模式

直接运行 Python 脚本查看详细输出：
```bash
cd license_launcher
python3.11 launcher.py
```

### 常用命令

```bash
# 查看文件大小
ls -lh dist/lzy_zte_许可证验证

# 检查文件类型
file dist/lzy_zte_许可证验证

# 测试运行
./dist/lzy_zte_许可证验证

# 清理打包文件
rm -rf build dist *.spec
```

---

## 🎉 总结

### 已完成

- ✅ 成功打包 lzy_zte_12.10.exe
- ✅ 内嵌许可证验证功能
- ✅ 生成单个可执行文件
- ✅ 自动选择 GUI/CLI 模式
- ✅ 支持许可证保存和自动验证

### 分发文件

```
dist/lzy_zte_许可证验证        # 35 MB
```

### 下一步

1. 测试程序功能
2. 准备生产环境服务器地址
3. 重新打包（使用生产配置）
4. 分发给用户

---

**创建时间**: 2025-12-14
**打包工具**: PyInstaller 6.17.0
**Python 版本**: 3.11.14
**平台**: macOS arm64
