# 🚀 集成快速参考

## 📦 三种集成方式

### 方式 1️⃣: 完整独立部署 (最简单)

```bash
# 1. 使用快速集成脚本
./快速集成.sh ~/Desktop/LicenseServer

# 2. 启动服务器
cd ~/Desktop/LicenseServer/server
./server

# 3. 访问管理界面
# http://localhost:8080/login.html
```

**适合**: 需要完整许可证管理系统的独立服务

---

### 方式 2️⃣: 集成到现有项目

```bash
# 1. 集成到你的 Go 项目
./快速集成.sh /path/to/your/project

# 2. 修改你的 main.go
```

```go
import (
    "yourproject/license/database"
    "yourproject/license/handlers"
)

func main() {
    // 初始化许可证数据库
    database.InitDB("./licenses.db")
    defer database.Close()

    // 注册许可证路由
    http.HandleFunc("/api/activate", handlers.HandleActivate)
    http.HandleFunc("/api/admin/license", handleAdminLicense)
    // ... 更多路由

    // 前端静态文件
    fs := http.FileServer(http.Dir("./license/frontend"))
    http.Handle("/license/", http.StripPrefix("/license/", fs))

    http.ListenAndServe(":8080", nil)
}
```

**适合**: 已有 Go 项目,想添加许可证功能

---

### 方式 3️⃣: 仅使用 API (无集成)

```bash
# 1. 启动许可证服务器 (独立运行)
cd server
go run main.go

# 2. 在你的应用中调用 API
```

**C# 示例**:
```csharp
var client = new HttpClient();
var data = new { license_key = "xxx", hwid = "yyy" };
var json = JsonSerializer.Serialize(data);
var response = await client.PostAsync(
    "http://localhost:8080/api/activate",
    new StringContent(json, Encoding.UTF8, "application/json")
);
```

**Python 示例**:
```python
import requests
response = requests.post(
    "http://localhost:8080/api/activate",
    json={"license_key": "xxx", "hwid": "yyy"}
)
```

**适合**: 任何语言的应用,通过 HTTP API 验证许可证

---

## 📚 完整文档

- 📖 [集成指南.md](./集成指南.md) - 详细集成说明
- 📖 [示例项目.md](./示例项目.md) - 代码示例和使用流程
- 📖 [README.md](./README.md) - 项目介绍和 API 文档

---

## 🎯 快速功能对照表

| 功能 | 方式1 独立部署 | 方式2 集成项目 | 方式3 纯API |
|------|:------------:|:------------:|:----------:|
| 许可证管理 | ✅ | ✅ | ✅ |
| Web 管理界面 | ✅ | ✅ | ✅ |
| 嵌入到应用 | ❌ | ✅ | ❌ |
| 独立运行 | ✅ | ❌ | ✅ |
| 跨语言支持 | ✅ | ❌ | ✅ |
| 自定义路由 | ❌ | ✅ | ✅ |

---

## 🔌 API 端点速查

### 客户端 API
```bash
POST /api/activate      # 激活许可证
POST /api/heartbeat     # 心跳验证
```

### 管理 API
```bash
POST   /api/admin/license   # 生成许可证
GET    /api/admin/license   # 获取许可证详情
PUT    /api/admin/license   # 更新许可证
DELETE /api/admin/license   # 删除许可证
GET    /api/admin/licenses  # 获取许可证列表
GET    /api/admin/stats     # 统计数据
```

### 管理界面
```
/login.html    # 登录页面
/index.html    # 管理后台
/test.html     # API 测试页面
```

---

## ⚡ 一键命令

```bash
# 查看集成选项
./快速集成.sh --help

# 完整部署到桌面
./快速集成.sh ~/Desktop/LicenseServer

# 集成到现有项目
./快速集成.sh ~/MyGoProject

# 启动本地测试
cd server && go run main.go
```

---

## 🔐 默认登录信息

```
用户名: lazywords
密码: w7168855
```

⚠️ **生产环境请务必修改密码!**

编辑 `frontend/login.html`:
```javascript
const validUsers = {
    'admin': 'your_secure_password'
};
```

---

## 📞 需要帮助?

1. 查看 [集成指南.md](./集成指南.md) 获取详细步骤
2. 查看 [示例项目.md](./示例项目.md) 获取代码示例
3. 在 GitHub 提交 Issue

---

## 🎉 快速验证

集成完成后,测试是否工作:

```bash
# 1. 启动服务器
go run main.go

# 2. 测试 API
curl http://localhost:8080/api/admin/stats

# 3. 访问界面
open http://localhost:8080/login.html
```

看到登录界面 = 成功! 🎊
